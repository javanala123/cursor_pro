# 🎯 선물 "데이터 없음" 근본 원인 최종 분석

## 📊 스크린샷 종합 분석

### 성공한 심볼들
| 심볼 | 거래수 | 수익 | 상태 |
|------|--------|------|------|
| GC1! | 10 | +17.02% | ✅ |
| USTEC.F | 54 | +42.34% | ✅ |
| WTI1! | 101 | +1.46% | ✅ |

### 실패한 심볼
| 심볼 | 거래수 | 상태 | 메시지 |
|------|--------|------|--------|
| XAUUSD | 0 | ❌ | "데이터 없음" |

## 🔍 "데이터 없음"의 의미

TradingView에서 "데이터 없음"은 다음을 의미합니다:
1. **전략이 실행되지 않음** (로딩 실패)
2. **데이터 피드 문제** (심볼 데이터 없음)
3. **브로커/거래소 제약** (해당 심볼 거래 불가)
4. **런타임 오류** (전략 실행 중 크래시)

## 🚨 진짜 근본 원인 발견

### 원인 1: **POA 라이브러리의 심볼 제약**

```pine
import dokang/POA/13 as POA
pstrategy = POA.bot.new(poa_password, start_time, end_time, leverage = ...)
pstrategy.start_trade()
```

**POA (Passive Order Automation) 라이브러리**:
- 특정 거래소/브로커와 연동
- 지원하는 심볼이 제한적일 가능성
- XAUUSD를 지원하지 않을 수 있음

**검증 방법**:
```
1. 원본 파일로 XAUUSD 테스트
2. POA 없이 순수 Pine Script로 테스트
```

### 원인 2: **XAUUSD의 데이터 피드 이슈**

**XAUUSD vs GC1!**:
- **GC1!**: COMEX 금 선물, 표준 거래소 데이터
- **XAUUSD**: 금/달러 CFD 또는 FX, 브로커마다 다름

**문제**:
- TradingView에서 XAUUSD 데이터가 일부 기간 누락
- 백테스트 기간 (2010~2049) 중 데이터 없음
- 특정 브로커(FOREX.com)만 제공

### 원인 3: **계약 가치 산출 오류**

```pine
else if str.contains(sym, "XAUUSD") or sym == "GOLD/USD"
    cv := 100.0  // 금 FX: 일반적으로 $100/온스
```

**문제**:
- XAUUSD는 $100/온스가 맞나?
- 실제로는 브로커마다 다름:
  - **표준 랏**: 100 온스 = $100/포인트
  - **미니 랏**: 10 온스 = $10/포인트
  - **마이크로 랏**: 1 온스 = $1/포인트

**계산 예시** (자금 $1,000,000, 가격 $2,660):
```
cv = 100
raw_contracts = 1000000 / (2660 × 100)
              = 1000000 / 266000
              = 3.76 계약

stepped = floor(3.76 / 0.1) × 0.1 = 3.7 계약

포지션 가치:
3.7 × 2660 × 100 = $984,200 ✅
```

**하지만** 만약 XAUUSD의 실제 계약 가치가 다르다면?
- cv = 1.0 (마이크로 랏): 376 계약 → $1,000,560 ✅
- cv = 10.0 (미니 랏): 37.6 계약 → $1,000,560 ✅
- cv = 1000.0 (잘못된 값): 0.376 → 0.3 계약 → $798,000

### 원인 4: **전략 초기화 실패**

```pine
strategy("볼린저밴드돌파_카피",
         overlay=true, 
         initial_capital=1000, 
         commission_type=strategy.commission.percent, 
         commission_value=0.05,
         margin_long=0.1,
         margin_short=0.1,
         use_bar_magnifier=false)
```

**margin_long=0.1, margin_short=0.1** = **10배 레버리지**

**XAUUSD에서**:
```
진입: 3.7 계약
포지션: $984,200
필요 마진: $984,200 / 10 = $98,420
자금: $1,000 ❌

→ 마진 부족!
```

**아! 이게 원인입니다!**

`initial_capital=1000` (천 달러)인데, XAUUSD 1 계약만 해도 $2,660 × 100 = $266,000입니다!

10배 레버리지로도:
- 필요 마진: $266,000 / 10 = $26,600
- 자금: $1,000
- **$1,000으로는 0.01 계약도 불가능!**

## ✅ 해결 방안

### 방안 1: **초기 자본 증가**

```pine
strategy("...", initial_capital=100000, ...)  // $100,000
```

**XAUUSD 계산** (cv=100, 가격=$2,660):
```
자금: $100,000
raw_contracts = 100000 / (2660 × 100) = 0.376
stepped = floor(0.376 / 0.1) × 0.1 = 0.3
필요 마진: 0.3 × 2660 × 100 / 10 = $7,980 ✅
```

### 방안 2: **XAUUSD 계약 가치 수정**

XAUUSD가 **마이크로 랏**(1온스)이라면:
```pine
else if str.contains(sym, "XAUUSD")
    cv := 1.0  // 마이크로 랏: $1/온스
```

**계산** (자금=$1,000):
```
raw_contracts = 1000 / (2660 × 1.0) = 0.376
stepped = 0.3
포지션: 0.3 × 2660 × 1.0 = $798
필요 마진: $798 / 10 = $79.8 ✅
```

### 방안 3: **최소 계약 조건 추가**

```pine
if market_type == "선물"
    cv = get_contract_value()
    contract_notional = close * cv
    required_margin = contract_notional * futures_min_lot / 10  // 10배 레버리지 가정
    
    // 자금이 부족하면 진입 스킵
    if strategy.equity < required_margin
        entry_qty := 0
    else
        raw_contracts = lot_size / contract_notional
        stepped = math.floor(raw_contracts / futures_lot_step) * futures_lot_step
        entry_qty := stepped > 0 ? stepped : futures_min_lot
```

### 방안 4: **심볼별 최소 자금 경고**

설정 테이블에 추가:
```pine
min_capital_required = close * get_contract_value() * futures_min_lot / 10
sufficient_capital = strategy.equity >= min_capital_required

table.cell(..., text="최소 자금: $" + str.tostring(min_capital_required) + 
                      "\n현재 자금: $" + str.tostring(strategy.equity) +
                      "\n상태: " + (sufficient_capital ? "✅" : "❌ 부족"))
```

## 🎯 최종 결론

**"데이터 없음"의 진짜 원인**:

1. ❌ **계약 가치 감지 문제 아님** (이미 100으로 설정됨)
2. ❌ **수량 계산 로직 문제 아님** (정상 작동)
3. ✅ **초기 자본 부족** (initial_capital=1000)
   - XAUUSD 최소 0.1 계약 = $266,000 × 0.1 = $26,600
   - 10배 레버리지 마진 = $2,660
   - **$1,000으로는 거래 불가능!**

4. ✅ **POA 라이브러리 제약** (가능성)
   - XAUUSD를 지원하지 않을 수 있음
   - 특정 브로커만 연동 가능

### 즉시 테스트

```
1. initial_capital=100000으로 변경
2. XAUUSD 재백테스트
3. 여전히 "데이터 없음"이면 → POA 제약
4. 거래가 발생하면 → 자본 문제 해결!
```

---

**핵심**: `initial_capital=1000`는 XAUUSD 같은 고가 심볼에는 **턱없이 부족**합니다!

