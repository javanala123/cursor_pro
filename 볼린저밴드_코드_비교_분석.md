# 볼린저 밴드 돌파 전략 코드 비교 분석

## 📋 목차
1. [파일 개요](#파일-개요)
2. [주요 차이점](#주요-차이점)
3. [상세 비교표](#상세-비교표)
4. [기능별 비교](#기능별-비교)
5. [권장 사용 시나리오](#권장-사용-시나리오)

---

## 1. 파일 개요

### 1.1 비교 대상 파일

| 파일명 | 설명 | 라인 수 | 특징 |
|--------|------|---------|------|
| **볼린져밴드_돌파_선물전용_NoPOA_Full.pine** | 원본 버전 | 530 | POA 제거, 간소화된 AI 필터, 선물 전용 |
| **볼린져밴드_돌파_원본복사_선물지원.pine** | 현재 작업 버전 | 852 | POA 포함, 완전한 AI 필터, 코인/선물 양방향 |

---

## 2. 주요 차이점

### 2.1 핵심 차이점 요약

| 구분 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **POA 라이브러리** | ❌ 제거됨 | ✅ 포함됨 |
| **AI Trend Filter** | ⚠️ 간소화 (항상 true) | ✅ 완전 구현 (KNN 알고리즘) |
| **시장 지원** | 선물 전용 (코인 선택 가능) | 코인/선물 양방향 |
| **대시보드** | 간소화 (월별 성과만) | 상세 (통계, 승률, 롱/숏 분리) |
| **TP/SL 계산** | 진입 시점 ATR 고정 | 실시간 ATR 업데이트 |
| **하이키야시 지원** | ❌ 없음 | ✅ 있음 |
| **코드 복잡도** | 낮음 (530줄) | 높음 (852줄) |

---

## 3. 상세 비교표

### 3.1 POA 라이브러리 관련

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **POA Import** | ❌ 없음 | ✅ `import dokang/POA/13 as POA` |
| **POA 설정** | ❌ 없음 | ✅ 시작/종료 시간, 비밀번호, 레버리지 |
| **코인 거래** | `strategy.entry()` 직접 사용 | `pstrategy.entry()` 사용 |
| **선물 거래** | `strategy.entry()` 직접 사용 | `strategy.entry()` 사용 |

**영향**: 
- 원본: 순수 Pine Script만 사용, TradingView 백테스팅 전용
- 현재: POA 봇 연동 가능, 실제 거래소 자동매매 지원

### 3.2 AI Trend Filter 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **구현 상태** | 간소화 (항상 true) | 완전 구현 (KNN 알고리즘) |
| **KNN 알고리즘** | ❌ 없음 | ✅ 있음 |
| **가격 값 옵션** | ❌ 없음 | ✅ hl2/VWAP/sma/wma/ema/hma |
| **타겟 값 옵션** | ❌ 없음 | ✅ Price Action/VWAP/Volatility/sma/wma/ema/hma |
| **KNN 파라미터** | ❌ 없음 | ✅ numberOfClosestValues, smoothingPeriod |
| **시각화** | ❌ 없음 | ✅ Knn Classifier Line, 배경색 표시 |

**코드 비교:**

**원본 버전:**
```pine
// AI Trend (간소화 - KNN 제거)
ait_enable_long = input.bool(false, "Enable Long", inline="ait1", group="AI Trend Filter")
ait_enable_short = input.bool(false, "Enable Short", inline="ait1", group="AI Trend Filter")

bool ait_long_cond = true
bool ait_short_cond = true
```

**현재 버전:**
```pine
// ai trend (원본 그대로)
ait_enable_long = input.bool(false, title = "Enable Long     ", group="AI Trend Filter", inline="ait_onoff")
ait_enable_short = input.bool(false, title = "Enable Short", group="AI Trend Filter", inline="ait_onoff")
PriceValue   = input.string("hl2", options = ["hl2","VWAP", "sma", "wma", "ema", "hma"], group="AI Trend Filter", inline="Value")
maLen        = input.int(5, minval=2, maxval=200, title="", group="AI Trend Filter", inline="Value")
TargetValue  = input.string("Price Action", options = ["Price Action","VWAP", "Volatility", "sma", "wma", "ema", "hma"], group="AI Trend Filter", inline="Target")
// ... KNN 알고리즘 구현 (약 70줄) ...
ait_long_cond = ait_enable_long ? red : true 
ait_short_cond = ait_enable_short ? green : true
```

**영향**:
- 원본: AI 필터링 효과 없음, 항상 통과
- 현재: KNN 기반 추세 예측으로 신호 품질 향상 가능

### 3.3 시장 지원 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **기본 시장** | 선물 | 코인 |
| **코인 거래** | ✅ 지원 (strategy 사용) | ✅ 지원 (POA 사용) |
| **선물 거래** | ✅ 지원 (strategy 사용) | ✅ 지원 (strategy 사용) |
| **하이키야시** | ❌ 없음 | ✅ 있음 |
| **가격 소스** | `close` 고정 | `close` 또는 하이키야시 종가 |

**코드 비교:**

**원본 버전:**
```pine
// 수량 계산 함수
calc_qty(amount) =>
    if market_type == "코인"
        math.round(amount / close, exchange_decimal)
    else
        cv = final_contract_value
        raw = amount / (close * cv)
        stepped = math.floor(raw / futures_lot_step) * futures_lot_step
        stepped > 0 ? stepped : futures_min_lot
```

**현재 버전:**
```pine
// ========== 하이키야시 가격 소스 계산 ==========
get_price_source() =>
    if use_heikin_ashi
        ha_close = (open + high + low + close) / 4.0
        ha_close
    else
        close

price_src = get_price_source()

calc_qty(amount) =>
    if market_type == "코인"
        math.round(amount / price_src, exchange_decimal)
    else
        cv = final_contract_value
        raw = amount / (price_src * cv)
        stepped = math.floor(raw / futures_lot_step) * futures_lot_step
        stepped > 0 ? stepped : futures_min_lot
```

**영향**:
- 원본: 일반 캔들만 사용
- 현재: 하이키야시 캔들 지원으로 노이즈 필터링 가능

### 3.4 대시보드 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **월별 성과표** | ✅ 있음 (간소화) | ✅ 있음 (상세) |
| **상태 테이블** | ❌ 없음 | ✅ 있음 (승률, 수익/손실 분리) |
| **설정 테이블** | ✅ 있음 (간소화) | ✅ 있음 (상세) |
| **롱/숏 분리 통계** | ❌ 없음 | ✅ 있음 |
| **수익 팩터** | ❌ 없음 | ✅ 있음 |
| **최대 낙폭** | ❌ 없음 | ✅ 있음 |

**원본 버전 대시보드:**
- 월별 성과표 (간소화)
- 설정값 뷰 (8개 항목)

**현재 버전 대시보드:**
- 상태 테이블 (6개 행, 상세 통계)
- 월별 성과표 (상세)
- 설정 테이블 (5개 행, 상세 설정)

### 3.5 TP/SL 계산 방식 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **ATR 사용 시점** | 진입 시점 고정 | 실시간 업데이트 |
| **ATR 변수** | `atr = ta.atr(atrLength)` (전역) | `openAtr = ta.valuewhen(...)` (진입 시점) |
| **TP/SL 업데이트** | 진입가 기준 고정 | 포지션 유지 중 업데이트 가능 |
| **트레일링 스톱** | TP/ON 모드 | OFF/ON/TP 모드 |

**코드 비교:**

**원본 버전:**
```pine
// ATR
atrLength = input.int(14, "ATR Length", group="TAKE PROFIT / Stop Loss")
atr = ta.atr(atrLength)  // 전역 ATR (항상 현재 값)

// TP/SL 계산
longTakeProfitPrice = 0.0
longTakeProfitPrice := if (longtakeProfitMethod == "PERC ")
    longEntryPrice * (1 + longTakeProfitPerc)
else if (longtakeProfitMethod == "ATR ")
    longEntryPrice + atr * longTakeProfitAtrMul  // 현재 ATR 사용
else
    longEntryPrice + math.max(longEntryPrice * longTakeProfitPerc, atr * longTakeProfitAtrMul)
```

**현재 버전:**
```pine
// SHARED VARIABLES
atrLength = input.int(defval = 14, title = 'ATR Length ', minval = 1, inline = 'ATR', group = 'General')

var bool longTrailingTakeProfitExecuted = false
var bool shortTrailingTakeProfitExecuted = false

float openAtr = ta.valuewhen(validOpenLongPosition or validOpenShortPosition, ta.atr(atrLength), 0)  // 진입 시점 ATR 저장

getLongTakeProfitPrice() =>
    switch longtakeProfitMethod
        'PERC' => close * (1 + longTakeProfitPerc)
        'ATR' => close + longTakeProfitAtrMul * openAtr  // 진입 시점 ATR 사용
        'BOTH' => math.max(close * (1 + longTakeProfitPerc), close + longTakeProfitAtrMul * openAtr)
        => na
```

**영향**:
- 원본: 현재 시장 변동성에 따라 TP/SL이 변할 수 있음
- 현재: 진입 시점의 변동성을 기준으로 고정 (일관성 유지)

### 3.6 선물 거래 로직 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **거래 로직** | 통합 (코인/선물 동일) | 분리 (코인/선물 독립) |
| **수량 계산** | `calc_qty()` 함수 사용 | 선물 전용 블록에서 별도 계산 |
| **디버깅 라벨** | ❌ 없음 | ✅ 있음 (진입 정보 표시) |
| **에러 처리** | ❌ 없음 | ✅ 있음 (안전성 체크) |

**코드 비교:**

**원본 버전:**
```pine
// LOGIC
if (closeLongPosition)
    strategy.close(id = 'Long Entry', comment = 'TR Long', immediately = true)

if (validOpenLongPosition)
    strategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
    qty = calc_qty(lot_size)  // 코인/선물 공통 함수
    strategy.entry(id = 'Long Entry', direction = strategy.long, qty = qty)
    long_position_size := qty
```

**현재 버전:**
```pine
// 코인 거래
if (validOpenLongPosition)
    if market_type == "코인"
        pstrategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
        coin_lot_size = long_size_type(long_type)
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = math.round(coin_lot_size / price_src, exchange_decimal))
        long_position_size := coin_lot_size / price_src

// 선물 거래 (완전히 분리)
if market_type == "선물"
    if (futures_validOpenLongPosition)
        strategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
        cv = final_contract_value
        contract_value = close * cv
        // 안전성 체크 및 디버깅 라벨
        raw_qty = futures_lot_size_usd / contract_value
        rounded_qty = math.floor(raw_qty / futures_lot_step) * futures_lot_step
        final_qty = math.max(rounded_qty, futures_min_lot)
        // 디버깅 라벨 표시
        strategy.entry(id = 'Long Entry', direction = strategy.long, qty = final_qty)
        long_position_size := final_qty
```

**영향**:
- 원본: 간단하지만 디버깅 어려움
- 현재: 복잡하지만 디버깅 용이, 에러 처리 강화

### 3.7 ARA 필터 변수 선언 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **변수 선언** | `var float ma = na` | `ma = 0.` |
| **타입 안정성** | ✅ 명시적 타입 | ⚠️ 암시적 타입 |

**코드 비교:**

**원본 버전:**
```pine
var float ma = na
var float mad = na
float src_ = close
ma := nz(mad[1], src_)
float d = ta.cum(math.abs(src_[ara_length] - ma)) / bar_index * gamma
```

**현재 버전:**
```pine
ma = 0.
mad = 0.

src_ = close
ma := nz(mad[1], src_)
d = ta.cum(math.abs(src_[ara_length] - ma)) / bar_index * gamma
```

**영향**:
- 원본: 타입 안정성 높음, 초기값 명확
- 현재: 타입 추론에 의존, 초기값이 0

### 3.8 ADX 필터 변수 선언 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **변수 선언** | `var float` 사용 | `0.0` 초기화 후 `:=` 사용 |
| **타입 명시** | ✅ 명시적 | ⚠️ 암시적 |

**코드 비교:**

**원본 버전:**
```pine
var float long_SmoothedTrueRange = 0.0
long_SmoothedTrueRange := nz(long_SmoothedTrueRange[1]) - nz(long_SmoothedTrueRange[1]) / long_adxlen + TrueRange

var float long_SmoothedDirectionalMovementPlus = 0.0
long_SmoothedDirectionalMovementPlus := nz(long_SmoothedDirectionalMovementPlus[1]) - nz(long_SmoothedDirectionalMovementPlus[1]) / long_adxlen + DirectionalMovementPlus

float long_DIPlus = long_SmoothedDirectionalMovementPlus / long_SmoothedTrueRange * 100
float long_DIMinus = long_SmoothedDirectionalMovementMinus / long_SmoothedTrueRange * 100
float long_DX = math.abs(long_DIPlus - long_DIMinus) / (long_DIPlus + long_DIMinus) * 100
float long_ADX = ta.sma(long_DX, long_adxlen)
```

**현재 버전:**
```pine
long_SmoothedTrueRange = 0.0
long_SmoothedTrueRange := nz(long_SmoothedTrueRange[1]) - nz(long_SmoothedTrueRange[1]) / long_adxlen + TrueRange

long_SmoothedDirectionalMovementPlus = 0.0
long_SmoothedDirectionalMovementPlus := nz(long_SmoothedDirectionalMovementPlus[1]) - nz(long_SmoothedDirectionalMovementPlus[1]) / long_adxlen + DirectionalMovementPlus

long_DIPlus = long_SmoothedDirectionalMovementPlus / long_SmoothedTrueRange * 100
long_DIMinus = long_SmoothedDirectionalMovementMinus / long_SmoothedTrueRange * 100
long_DX = math.abs(long_DIPlus - long_DIMinus) / (long_DIPlus + long_DIMinus) * 100
long_ADX = ta.sma(long_DX, long_adxlen)
```

**영향**:
- 원본: `var` 키워드로 상태 유지 명확
- 현재: `var` 없이도 동작하지만 명시성이 떨어짐

---

## 4. 기능별 비교

### 4.1 공통 기능

| 기능 | 원본 버전 | 현재 버전 | 비고 |
|------|----------|----------|------|
| 볼린저 밴드 계산 | ✅ | ✅ | 동일 |
| Volume Filter | ✅ | ✅ | 동일 |
| ADX Filter | ✅ | ✅ | 동일 |
| Volatility Filter | ✅ | ✅ | 동일 |
| Historical Volatility | ✅ | ✅ | 동일 |
| Volume OSC | ✅ | ✅ | 동일 |
| ARA Filter | ✅ | ✅ | 동일 |
| 손절/익절 | ✅ | ✅ | 계산 방식 약간 다름 |
| 트레일링 스톱 | ✅ | ✅ | 모드 차이 |
| 브레이크이븐 | ✅ | ✅ | 동일 |

### 4.2 원본 버전에만 있는 기능

없음 (현재 버전이 원본의 상위 집합)

### 4.3 현재 버전에만 있는 기능

| 기능 | 설명 | 중요도 |
|------|------|--------|
| **POA 라이브러리** | 실제 거래소 자동매매 연동 | ⭐⭐⭐⭐⭐ |
| **KNN AI 필터** | 완전한 AI 추세 예측 | ⭐⭐⭐⭐ |
| **하이키야시 지원** | 노이즈 필터링 | ⭐⭐⭐ |
| **상세 대시보드** | 롱/숏 분리 통계, 수익 팩터 등 | ⭐⭐⭐ |
| **디버깅 라벨** | 선물 거래 시 진입 정보 표시 | ⭐⭐ |
| **에러 처리** | 선물 거래 안전성 체크 | ⭐⭐⭐ |

---

## 5. 권장 사용 시나리오

### 5.1 원본 버전 사용 권장

✅ **다음 상황에서 원본 버전 권장:**

1. **TradingView 백테스팅만 필요**
   - POA 연동 불필요
   - 순수 Pine Script로 충분

2. **간단한 전략 테스트**
   - AI 필터 없이 기본 전략만 테스트
   - 빠른 실행 속도 필요

3. **코드 이해 및 학습**
   - 간단한 구조로 전략 이해
   - 복잡한 로직 제거로 학습 용이

4. **안정성 우선**
   - 복잡한 AI 알고리즘으로 인한 오류 우려
   - 검증된 간단한 로직 선호

### 5.2 현재 버전 사용 권장

✅ **다음 상황에서 현재 버전 권장:**

1. **실제 거래소 자동매매**
   - POA 봇 연동 필요
   - 실제 자금으로 거래

2. **AI 기반 필터링 필요**
   - KNN 알고리즘으로 추세 예측
   - 신호 품질 향상 필요

3. **상세한 통계 분석**
   - 롱/숏 분리 통계
   - 수익 팩터, 승률 등 상세 분석

4. **하이키야시 캔들 사용**
   - 노이즈 필터링 필요
   - 더 부드러운 신호 선호

5. **디버깅 및 모니터링**
   - 선물 거래 시 진입 정보 확인
   - 에러 상황 파악 필요

---

## 6. 마이그레이션 가이드

### 6.1 원본 → 현재 버전으로 전환 시

**추가 설정 필요:**
1. POA 비밀번호 설정
2. POA 레버리지 설정
3. AI Trend Filter 파라미터 튜닝
4. 하이키야시 옵션 설정 (선택)

**주의사항:**
- AI 필터 활성화 시 진입 빈도 감소 가능
- POA 연동 시 실제 거래 주의

### 6.2 현재 → 원본 버전으로 전환 시

**제거되는 기능:**
1. POA 연동 기능
2. KNN AI 필터
3. 하이키야시 지원
4. 상세 대시보드

**유지되는 기능:**
- 모든 기본 필터
- 손절/익절 로직
- 볼린저 밴드 계산

---

## 7. 성능 비교

| 항목 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **코드 라인 수** | 530줄 | 852줄 |
| **실행 속도** | 빠름 | 느림 (AI 계산) |
| **메모리 사용** | 낮음 | 높음 (KNN 배열) |
| **복잡도** | 낮음 | 높음 |

---

## 8. 결론

### 8.1 요약

- **원본 버전**: 간단하고 빠르며 안정적, 백테스팅에 적합
- **현재 버전**: 기능이 풍부하고 실전 거래에 적합, AI 필터 포함

### 8.2 선택 기준

| 기준 | 원본 버전 | 현재 버전 |
|------|----------|----------|
| **백테스팅** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **실전 거래** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **AI 필터** | ❌ | ⭐⭐⭐⭐⭐ |
| **안정성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **기능성** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### 8.3 최종 권장사항

- **학습 및 테스트**: 원본 버전
- **실전 거래**: 현재 버전
- **AI 필터 필요**: 현재 버전
- **간단함 선호**: 원본 버전

---

**작성일**: 2024년  
**비교 대상**: 
- 원본: `볼린져밴드_돌파_선물전용_NoPOA_Full.pine`
- 현재: `볼린져밴드_돌파_원본복사_선물지원.pine`

