# 🚨 선물 완전 실패 긴급 분석

## 현상

**사용자 피드백**:
> "지금 선물은 정상작동 하는데 하나도 없어...시간프레임을 바꿔가면서 해본 결과야"

- 모든 선물 심볼: 거래 0건
- 모든 시간프레임: 실패
- 코인: 정상 작동

## 🔍 핵심 의심 포인트

### 의심 1: `calc_order_qty`가 0 반환

**검증 코드 추가**:
```pine
var float debug_long_qty = na
var float debug_short_qty = na

if (validOpenLongPosition)
    long_qty = calc_order_qty(true)
    debug_long_qty := long_qty  // ← 이 값 확인!
```

**설정 테이블에 표시**:
```
마지막 계산 수량: L=? S=?
```

**예상**:
- 만약 `L=0` 또는 `L=na`이면 → `calc_order_qty` 버그
- 만약 `L=0.1`인데도 거래 안 되면 → POA 라이브러리 거부

### 의심 2: `amount`가 0 또는 na

**calc_order_qty 내부**:
```pine
float amount = isLong ? long_size_type(long_type) : short_size_type(short_type)
```

**long_size_type**:
```pine
long_size_type(user_input_style) =>
    user_input_style == "Cash " ? long_size * sumcapital : 
    user_input_style == "Percent " ? long_per_size : 
    na
```

**가능한 오류**:
1. `long_type` 값이 `"Cash "` 또는 `"Percent "`가 아님
2. `sumcapital`이 0 또는 na
3. `long_size`가 0
4. `long_per_size`가 0

### 의심 3: `market_type` 비교 실패

**현재 코드**:
```pine
if market_type == "코인"
    ...
else
    // 선물 로직
```

**가능한 오류**:
- `market_type`이 "코인"도 "선물"도 아닌 다른 값?
- 문자열 인코딩 문제?

### 의심 4: `futures_min_lot` 비교 문제

**현재 코드**:
```pine
lots = lots_stepped > 0 ? lots_stepped : futures_min_lot
lots  // 조건 없이 반환
```

**가능한 오류**:
- `futures_min_lot`이 0?
- `lots_stepped`와 `futures_min_lot` 모두 0?

### 의심 5: POA 라이브러리가 선물 거부

**가능성**:
```pine
pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
```

- POA 라이브러리가 내부적으로 심볼 타입 확인?
- 선물은 거부?
- 최소 주문 금액 검증?

## 🔬 체계적 디버깅 절차

### Step 1: `debug_long_qty` 확인

**백테스트 후 설정 테이블에서**:
```
시장 유형 / 계약가치
선물 / 자동: 2.0
심볼: USTEC.F
마지막 계산 수량: L=? S=?
```

**Case A**: `L=0` 또는 `L=na`
→ `calc_order_qty` 함수 버그
→ `amount`, `contract_notional`, `raw_qty` 확인 필요

**Case B**: `L=0.1` (정상)
→ `pstrategy.entry` 호출됐는데도 거래 안 됨
→ POA 라이브러리 문제

### Step 2: `amount` 계산 확인

**테스트 코드 추가**:
```pine
var float debug_amount = na
var float debug_cv = na
var float debug_contract_notional = na
var float debug_raw_qty = na

calc_order_qty(isLong) =>
    float amount = isLong ? long_size_type(long_type) : short_size_type(short_type)
    debug_amount := amount  // ← 디버깅
    
    if market_type == "코인"
        math.round(amount / close, exchange_decimal)
    else
        cv = get_contract_value()
        debug_cv := cv  // ← 디버깅
        contract_notional = close * cv
        debug_contract_notional := contract_notional  // ← 디버깅
        raw_qty = amount / contract_notional
        debug_raw_qty := raw_qty  // ← 디버깅
        ...
```

**설정 테이블에 표시**:
```
디버그: amount=? cv=? notional=? raw_qty=?
```

### Step 3: `market_type` 확인

**테스트 코드**:
```pine
var string debug_market_type = na
debug_market_type := market_type
```

**설정 테이블에 표시**:
```
market_type: [선물] (확인용)
```

### Step 4: 원본 로직으로 롤백 테스트

**원본 방식**:
```pine
if (validOpenLongPosition)
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", 
                    qty = math.round(lot_size / close, exchange_decimal))
```

**테스트 버전**:
```pine
if (validOpenLongPosition)
    if market_type == "코인"
        qty_test = math.round(lot_size / close, exchange_decimal)
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = qty_test)
    else
        qty_test = calc_order_qty(true)
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = qty_test)
```

## 💡 가장 의심되는 원인

### 원인 1: `lot_size` 변수 부재 (가능성 90%)

**원본 코드**:
```pine
lot_size = validOpenLongPosition ? long_size_type(long_type) : 
           validOpenShortPosition ? short_size_type(short_type) : na
```

**수정본 코드**:
```pine
// lot_size 변수가 없음! ❌
```

**영향**:
- 원본: `lot_size`를 먼저 계산
- 수정본: `calc_order_qty` 내부에서 계산
- 하지만 다른 곳에서 `lot_size` 참조 가능성!

**해결책**:
```pine
// 원본과 동일하게 lot_size 계산
lot_size = validOpenLongPosition ? long_size_type(long_type) : 
           validOpenShortPosition ? short_size_type(short_type) : na
```

### 원인 2: `sumcapital` 계산 오류

**현재 코드**:
```pine
sumcapital = profit_balance ? strategy.initial_capital + strategy.netprofit : 
                               strategy.initial_capital
```

**가능한 문제**:
- 첫 바에서 `strategy.netprofit`이 na?
- `sumcapital = 0`?

**해결책**:
```pine
sumcapital = profit_balance ? 
             nz(strategy.initial_capital + strategy.netprofit, strategy.initial_capital) : 
             strategy.initial_capital
```

### 원인 3: 선물 모드에서 `exchange_decimal` 적용 안 됨

**원본**:
```pine
qty = math.round(lot_size / close, exchange_decimal)
```

**수정본 선물 모드**:
```pine
// exchange_decimal 사용 안 함!
lots = lots_stepped > 0 ? lots_stepped : futures_min_lot
```

**문제**:
- 선물도 소수점 자릿수 제한 필요?
- `lots = 0.1`인데 `exchange_decimal = 3`이면 OK
- 하지만 `exchange_decimal = 0`이면?

## 🎯 즉시 적용할 해결책

### 해결책 1: `lot_size` 변수 복원

```pine
// calc_order_qty 호출 전에 lot_size 계산
lot_size = validOpenLongPosition ? long_size_type(long_type) : 
           validOpenShortPosition ? short_size_type(short_type) : na

var float long_position_size = 0
var float short_position_size = 0

if (validOpenLongPosition)
    pstrategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
    long_qty = calc_order_qty(true)
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
    long_position_size := long_qty
```

### 해결책 2: 원본 로직 병행 사용

```pine
if (validOpenLongPosition)
    pstrategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
    
    if market_type == "코인"
        // 원본 방식
        qty_calc = math.round(lot_size / close, exchange_decimal)
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = qty_calc)
        long_position_size := qty_calc
    else
        // 선물 방식
        long_qty = calc_order_qty(true)
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
        long_position_size := long_qty
```

### 해결책 3: 강제 최소값 보정

```pine
calc_order_qty(isLong) =>
    ...
    else
        cv = get_contract_value()
        contract_notional = close * cv
        raw_qty = amount / contract_notional
        lots_stepped = math.floor(raw_qty / futures_lot_step) * futures_lot_step
        
        // 강제 보정: 절대 0 반환 안 함!
        lots = lots_stepped > 0 ? lots_stepped : futures_min_lot
        
        // 추가 안전장치
        lots < 0.01 ? 0.1 : lots  // 0.01 미만이면 강제 0.1
```

## 📋 긴급 체크리스트

- [ ] 설정 테이블에서 `L=?` 값 확인
- [ ] `L=0`이면 → `calc_order_qty` 버그
- [ ] `L=0.1`인데도 거래 안 되면 → POA 문제
- [ ] 원본 파일과 100% 동일한 부분 확인
- [ ] `lot_size` 변수 존재 여부 확인
- [ ] `sumcapital` 값 확인
- [ ] `market_type` 정확성 확인

---

**다음 단계**: 
1. 백테스트 실행
2. 설정 테이블 확인: `L=?`
3. 결과에 따라 해결책 적용

