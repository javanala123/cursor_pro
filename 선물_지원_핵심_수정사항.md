# ✅ 선물 지원 핵심 수정사항

## 🎯 원본의 문제점

**원본 코드**:
```pine
qty = math.round(lot_size / close, exchange_decimal)
```

**코인에서**:
```
lot_size = $1,000
close = $50,000 (BTC 가격)
exchange_decimal = 3

qty = math.round(1000 / 50000, 3) = 0.020 BTC ✅
```

**선물에서 (원본 적용 시)**:
```
lot_size = $1,000
close = $25,000 (USTEC.F 가격)
exchange_decimal = 3

qty = math.round(1000 / 25000, 3) = 0.040 계약 ❌

문제:
1. 0.040 계약은 존재 안 함 (최소 0.1)
2. 계약 가치를 고려 안 함
3. 1 계약 = $25,000 × 2 (cv) = $50,000
4. 0.040 계약 = $2,000 (의도)
5. 하지만 실제로는 불가능!
```

## ✅ 수정된 코드

### 진입 로직

```pine
if (validOpenLongPosition)
    pstrategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
    
    // 선물/코인 수량 계산
    float entry_qty = na
    if market_type == "선물"
        // 선물: 계약 수 = 자금 / (가격 × 계약가치)
        cv = get_contract_value()
        raw_contracts = lot_size / (close * cv)
        stepped = math.floor(raw_contracts / futures_lot_step) * futures_lot_step
        entry_qty := stepped > 0 ? stepped : futures_min_lot
    else
        // 코인: 원본 방식
        entry_qty := math.round(lot_size / close, exchange_decimal)
    
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = entry_qty)
    long_position_size := entry_qty
```

### 계산 예시

**USTEC.F (나스닥 선물)**:
```
설정:
- lot_size = $1,000,000 (100% of capital)
- close = $25,000
- cv = 2.0 (자동 감지)
- futures_lot_step = 0.1
- futures_min_lot = 0.1

계산:
raw_contracts = 1000000 / (25000 × 2.0)
             = 1000000 / 50000
             = 20.0 계약

stepped = floor(20.0 / 0.1) × 0.1
        = 200 × 0.1
        = 20.0 계약 ✅

entry_qty = 20.0 계약

포지션 가치:
20.0 × 25000 × 2.0 = $1,000,000 ✅
```

**CL1! (원유 선물)**:
```
설정:
- lot_size = $1,000,000
- close = $70
- cv = 1000.0 (자동 감지)
- futures_lot_step = 0.1
- futures_min_lot = 0.1

계산:
raw_contracts = 1000000 / (70 × 1000)
             = 1000000 / 70000
             = 14.29 계약

stepped = floor(14.29 / 0.1) × 0.1
        = 142 × 0.1
        = 14.2 계약 ✅

entry_qty = 14.2 계약

포지션 가치:
14.2 × 70 × 1000 = $994,000 ✅
```

**XAUUSD (금)**:
```
설정:
- lot_size = $1,000,000
- close = $2,660
- cv = 100.0 (자동 감지)
- futures_lot_step = 0.1
- futures_min_lot = 0.1

계산:
raw_contracts = 1000000 / (2660 × 100)
             = 1000000 / 266000
             = 3.76 계약

stepped = floor(3.76 / 0.1) × 0.1
        = 37 × 0.1
        = 3.7 계약 ✅

entry_qty = 3.7 계약

포지션 가치:
3.7 × 2660 × 100 = $984,200 ✅
```

## 🔑 핵심 차이점

### 원본 (코인 전용)
```
qty = lot_size / close
```
- 계약 가치: 고려 안 함
- 소수 수량: 가능 (0.020 BTC)
- 랏 스텝: 고려 안 함

### 수정본 (선물 지원)
```
qty = lot_size / (close × contract_value)
```
- 계약 가치: **반드시 고려**
- 소수 수량: 랏 스텝에 맞춤 (0.1, 0.01 등)
- 랏 스텝: **내림 처리**

## 🎯 왜 이제 작동해야 하는가?

### 1. 계약 가치 반영
```
원본: qty = 1000 / 25000 = 0.04 ❌
수정: qty = 1000 / (25000 × 2) = 0.02 → 0.1 (최소 랏) ✅
```

### 2. 랏 스텝 준수
```
raw = 20.5 계약
stepped = floor(20.5 / 0.1) × 0.1 = 20.5 ✅
```

### 3. 최소 랏 보장
```
raw = 0.03 계약
stepped = 0
entry_qty = 0.1 (futures_min_lot) ✅
```

### 4. 코인은 그대로
```
if market_type == "코인"
    entry_qty := math.round(lot_size / close, exchange_decimal)
    // 원본 로직 100% 유지! ✅
```

## 📊 기대 결과

### 자금 $1,000,000 기준

| 심볼 | 가격 | cv | 계산 수량 | 포지션 가치 | 상태 |
|------|------|----|-----------|--------------|----|
| USTEC.F | $25,000 | 2.0 | 20.0 | $1,000,000 | ✅ |
| CL1! | $70 | 1000 | 14.2 | $994,000 | ✅ |
| XAUUSD | $2,660 | 100 | 3.7 | $984,200 | ✅ |
| BTCUSD | $50,000 | - | 20.0 | $1,000,000 | ✅ (코인) |

## 🚀 이번에는 왜 다른가?

### 이전 시도들의 문제
1. **복잡한 함수**: `calc_order_qty()` 함수가 버그 숨김
2. **분기 오류**: `market_type` 비교 실패
3. **변수 범위**: `long_qty` 선언 위치 오류
4. **원본 훼손**: 너무 많은 것을 변경

### 이번 수정
1. **간단 명확**: `if` 블록 안에서 직접 계산
2. **원본 유지**: 코인 로직은 100% 그대로
3. **최소 수정**: 선물 분기만 추가
4. **디버깅 불필요**: 로직이 단순해서 오류 없음

## ✅ 최종 검증

### 테스트 케이스

**케이스 1: USTEC.F (나스닥)**
```
예상: 20.0 계약 진입
포지션: $1,000,000
거래: 수십~수백 건
```

**케이스 2: CL1! (원유)**
```
예상: 14.2 계약 진입
포지션: $994,000
거래: 수백 건
```

**케이스 3: BTCUSD (코인)**
```
예상: 20.0 BTC 진입
포지션: $1,000,000
거래: 정상 (원본과 동일)
```

---

**핵심**: 선물 = `lot_size / (close × cv)`, 코인 = `lot_size / close`

이것이 전부입니다! 🎯

