# 🚨 선물 완전 실패 - 최종 디버깅 버전

## 💰 중요 정보

**자금**: $1,000,000 (백만 달러!)
→ 마진 부족은 절대 아님!

## 🔧 적용된 수정

### 수정 1: 코인/선물 진입 로직 완전 분리

**Before (문제)**:
```pine
if (validOpenLongPosition)
    long_qty = calc_order_qty(true)  // 모든 케이스
    pstrategy.entry(...)
```

**After (해결)**:
```pine
if (validOpenLongPosition)
    // 코인: 원본 방식 그대로
    if market_type == "코인"
        long_qty = math.round(lot_size / close, exchange_decimal)
    else
        // 선물: calc_order_qty 사용
        long_qty = calc_order_qty(true)
    
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
```

**이유**:
- 원본은 `lot_size / close` 직접 사용
- 수정본이 `calc_order_qty`로 통합했지만
- 혹시 `calc_order_qty` 내부에 버그가 있을 수 있음
- 코인은 원본 방식 복원 = 100% 안전

### 수정 2: 상세 디버깅 정보 추가

**설정 테이블 Row 4**:
```
시장 유형 / 계약가치
선물 / 자동: 2.0
심볼: USTEC.F
계산 수량: L=? S=?
amount=? capital=?
```

**색상 코딩**:
- 🟢 초록색: `L > 0` 또는 `S > 0` (정상)
- 🔴 빨간색: `L = 0` 또는 `S = 0` (문제!)

**디버깅 변수**:
- `debug_long_qty`: 계산된 롱 수량
- `debug_short_qty`: 계산된 숏 수량
- `debug_amount`: USD 금액 (lot_size 또는 long_size_type 결과)
- `sumcapital`: 현재 자금

## 🎯 테스트 방법

### Step 1: 백테스트 실행

```
1. 파일 재로드
2. 심볼: USTEC.F (나스닥)
3. 시장 유형: 선물
4. 시간프레임: 1시간
5. initial_capital: 1000000
6. 백테스트 실행
```

### Step 2: 설정 테이블 확인

**우측 상단 "Setting Table" 열기**

**Row 4 확인**:
```
시장 유형 / 계약가치
선물 / 자동: 2.0
심볼: USTEC.F
계산 수량: L=? S=?
amount=? capital=?
```

### Step 3: 진단

#### Case A: L=0, S=0 (빨간색)

**의미**: `calc_order_qty`가 0 반환!

**원인 가능성**:
1. `amount = 0` → `long_size_type` 버그
2. `contract_notional`이 너무 큼
3. `raw_qty < futures_min_lot`
4. `calc_order_qty` 내부 로직 오류

**다음 단계**: `amount` 값 확인
- `amount = 0` → `long_size_type` 문제
- `amount > 0` → `calc_order_qty` 로직 문제

#### Case B: L=0.1, S=na (초록색)

**의미**: 계산은 정상, 진입 시도함

**원인 가능성**:
1. POA 라이브러리가 거부
2. `pstrategy.entry` 호출 안 됨
3. 진입 조건 차단 (필터)

**다음 단계**: 
- 필터 모두 OFF
- 백테스트 재실행
- 여전히 0건이면 POA 문제

#### Case C: L=10.5, S=na (초록색)

**의미**: 계산 정상, 수량도 충분

**원인 가능성**:
1. 필터가 진입 차단
2. BB 신호 자체가 없음
3. `validOpenLongPosition`이 false

**다음 단계**:
- 차트에서 작은 삼각형(BB 신호) 확인
- 큰 삼각형(실제 진입) 확인
- 필터 상태 행 확인

## 💡 예상 시나리오

### 시나리오 1: amount = 0 (가장 의심)

**증상**:
```
계산 수량: L=0.1 S=na
amount=0 capital=1000000
```

**원인**: `long_size_type(long_type)` 버그

**가능성**:
```pine
long_size_type(user_input_style) =>
    user_input_style == "Cash " ? long_size * sumcapital : 
    user_input_style == "Percent " ? long_per_size : 
    na
```

- `long_type`이 "Cash " 또는 "Percent "가 아님?
- 공백 위치 문제? ("Cash" vs "Cash ")
- `sumcapital`이 0?

**해결**:
```pine
// 안전장치 추가
long_size_type(user_input_style) =>
    user_input_style == "Cash " ? math.max(long_size * sumcapital, 1000) : 
    user_input_style == "Percent " ? math.max(long_per_size, 1000) : 
    1000  // na 대신 기본값
```

### 시나리오 2: calc_order_qty 버그

**증상**:
```
계산 수량: L=0 S=na
amount=100000000 capital=1000000
```

**원인**: `calc_order_qty` 내부 계산 오류

**가능성**:
```pine
lots = lots_stepped > 0 ? lots_stepped : futures_min_lot
```

- `futures_min_lot = 0`?
- `lots_stepped`와 `futures_min_lot` 모두 0?

**해결**:
```pine
lots = lots_stepped > 0 ? lots_stepped : 0.1  // 하드코딩
```

### 시나리오 3: 필터 차단

**증상**:
```
계산 수량: L=0.1 S=na
amount=100000000 capital=1000000
거래: 0건
차트: 작은 삼각형 많음, 큰 삼각형 없음
```

**원인**: 필터가 진입 차단

**해결**: 모든 필터 OFF

### 시나리오 4: POA 라이브러리 거부

**증상**:
```
계산 수량: L=0.1 S=na
amount=100000000 capital=1000000
거래: 0건
필터: 모두 OFF
차트: 큰 삼각형 나타나야 하는데 없음
```

**원인**: POA 라이브러리가 선물 거부?

**해결**: 
```pine
// 코인 모드로 테스트
market_type = "코인"
```

코인 모드로 선물 심볼 테스트
→ 거래 발생하면: POA 선물 지원 안 함
→ 여전히 0건이면: 다른 문제

## 📋 긴급 체크리스트

### 백테스트 후

- [ ] 설정 테이블 Row 4 색상 확인
  - [ ] 🟢 초록색: 계산 정상
  - [ ] 🔴 빨간색: 계산 실패

- [ ] `amount` 값 확인
  - [ ] `amount = 0`: `long_size_type` 버그
  - [ ] `amount > 0`: 계산 정상

- [ ] `L` 또는 `S` 값 확인
  - [ ] `L = 0`: `calc_order_qty` 버그
  - [ ] `L = 0.1`: 계산 정상

- [ ] 거래 횟수 확인
  - [ ] 0건: POA 또는 필터 문제
  - [ ] 수십 건: 정상!

### 다음 단계

**Case 1**: `amount = 0`
→ `long_size_type` 수정 필요

**Case 2**: `amount > 0`, `L = 0`
→ `calc_order_qty` 수정 필요

**Case 3**: `amount > 0`, `L > 0`, 거래 0건
→ 필터 OFF 또는 POA 문제

## 🎯 최종 목표

### 성공 기준

```
계산 수량: L=0.1 S=na (초록색)
amount=100000000 capital=1000000
거래: 50+ 건
수익: ?%
```

---

**핵심**: 설정 테이블 Row 4의 색상과 값이 모든 것을 말해줍니다!

빨간색이면 → 계산 버그
초록색인데 0거래 → POA 또는 필터 문제

