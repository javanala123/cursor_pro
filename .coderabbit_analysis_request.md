# CodeRabbit 분석 요청: 선물 심볼별 매매 편차 문제

## 🎯 문제 현상

### 백테스트 결과 (2010-2025, 15년)

| 심볼 | 거래 횟수 | 작동 기간 | 상태 |
|------|-----------|-----------|------|
| DXY | 0건 | 전체 0 | ❌ 완전 실패 |
| GC1! | 10건 | 2020년 일부만 | ⚠️ 부분 작동 |
| USTEC.F | 54건 | 2019-2025 | ✅ 정상 |
| CL1! | 205건 | 2019-2025 | ✅✅ 매우 정상 |
| BTCUSD | 정상 | 전체 기간 | ✅ 코인 정상 |

### 의심되는 원인

1. **심볼 자동 감지 실패**:
   - DXY가 감지 안 됨?
   - GC1!이 GOLD로 감지 안 됨?

2. **계약 가치 오류**:
   - DXY 계약 가치가 없음?
   - GC1! 계약 가치가 너무 큼?

3. **랏 계산 실패**:
   - 특정 심볼에서 `lots = 0` 반환?
   - `futures_min_lot` 도달 실패?

4. **데이터 문제**:
   - DXY 데이터가 없음?
   - 특정 월 데이터 누락?

## 📋 분석 요청 사항

### 1. detect_contract_value() 함수 분석

**파일**: `볼린져밴드_돌파_Futures_Optimized.fine`
**라인**: 49-139

**확인 사항**:
- [ ] DXY 심볼 감지 로직 존재 여부
- [ ] GC1! vs GOLD 매칭 로직
- [ ] USTEC.F vs MNQ 매칭
- [ ] CL1! vs WTI 매칭
- [ ] 기본값(cv=1.0) 반환 케이스

**중점 체크**:
```pine
// DXY 관련 로직이 있는가?
if str.contains(sym, "DXY") ...  // ← 없을 가능성!

// GC1! 매칭
if str.contains(sym, "GC") or str.contains(sym, "GOLD")  // ← "GC1!" 매칭되나?
    cv := 100.0
```

### 2. calc_order_qty() 함수 분석

**파일**: `볼린져밴드_돌파_Futures_Optimized.fine`
**라인**: 158-182

**확인 사항**:
- [ ] `contract_notional = close × cv` 계산 결과
- [ ] `raw_qty` 범위별 분포
- [ ] `lots_stepped` 0 발생 빈도
- [ ] `futures_min_lot` 보정 로직 작동 여부

**시나리오별 계산**:
```
DXY (추정):
- close: 100 (인덱스 값)
- cv: 1.0 (기본값)
- capital: $1,000
→ raw_qty = 1000 / (100 × 1) = 10.0
→ lots_stepped = floor(10 / 0.1) × 0.1 = 10.0
→ lots = 10.0 ✅ (이론상 OK?)

GC1! (금):
- close: 4,200
- cv: 100.0
- capital: $1,000
→ raw_qty = 1000 / (4200 × 100) = 0.00238
→ lots_stepped = 0
→ lots = 0.1 ✅ (보정됨)

그런데 왜 안 되지?
```

### 3. 진입 조건 로직 분석

**파일**: `볼린져밴드_돌파_Futures_Optimized.fine`
**라인**: 628-639

**확인 사항**:
- [ ] `validOpenLongPosition` 발생 빈도
- [ ] `long_qty` 계산 결과
- [ ] `pstrategy.entry()` 호출 여부
- [ ] POA 라이브러리 내부 거부 가능성

**의심 포인트**:
```pine
if (validOpenLongPosition)
    long_qty = calc_order_qty(true)
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
```

- `long_qty = 0`이면 POA가 거부?
- `long_qty = 0.1`인데도 POA가 거부?

### 4. 심볼별 특성 분석

**확인 필요**:

| 심볼 | 타입 | 가격대 | 계약 가치 | 최소 랏 |
|------|------|--------|-----------|---------|
| DXY | 인덱스 | ~100 | ? | ? |
| GC1! | 선물 | ~4,200 | 100? | 0.1? |
| USTEC.F | 선물 | ~25,000 | 2? | 0.1? |
| CL1! | 선물 | ~70 | 1000? | 0.1? |

**의문점**:
- DXY는 인덱스인데 거래 가능한가?
- GC1!의 "1!"은 어떻게 매칭되나?
- USTEC.F는 MNQ와 같은가?

### 5. 필터 조건 분석

**가설**: 특정 심볼에서 필터가 더 엄격하게 작동?

**확인 사항**:
- [ ] DXY의 BB 신호 발생 빈도
- [ ] DXY의 AI Trend 필터 통과율
- [ ] DXY의 Volume 데이터 존재 여부
- [ ] DXY의 Volatility 값

**의심**:
- DXY는 인덱스라 Volume이 없을 수도?
- Volume 필터가 DXY를 완전 차단?

## 🔍 체계적 분석 절차

### Step 1: 심볼 감지 검증

```
DXY → detect_contract_value() → cv = ?
GC1! → detect_contract_value() → cv = ?
USTEC.F → detect_contract_value() → cv = ?
CL1! → detect_contract_value() → cv = ?
```

**예상 결과**:
- CL1! → cv=1000 ✅
- USTEC.F → cv=2 ✅
- GC1! → cv=100 or 1.0?
- DXY → cv=1.0 (기본값)?

### Step 2: 랏 계산 시뮬레이션

```
각 심볼의 평균 가격으로 계산:
DXY (100): raw_qty = 1000/(100×cv)
GC1! (4200): raw_qty = 1000/(4200×cv)
USTEC.F (25000): raw_qty = 1000/(25000×cv)
CL1! (70): raw_qty = 1000/(70×cv)
```

**기대**:
- 모두 0.1 이상이어야 함
- 0이면 버그!

### Step 3: 필터 영향도 분석

```
각 심볼별 필터 통과율:
- BB 신호: ?%
- AI Trend: ?%
- Volume: ?% ← DXY 의심!
- ARMA: ?%
```

### Step 4: POA 라이브러리 제약 확인

```
pstrategy.entry(qty = 0.1)이 거부되는 케이스:
- 최소 주문 금액 미달?
- 마진 부족?
- 심볼 지원 안 함?
```

## 🎯 우선순위 분석

1. **최우선**: `detect_contract_value()`에 DXY 로직 있는지
2. **2순위**: GC1!의 "1!" 매칭 문제
3. **3순위**: Volume 데이터 존재 여부 (인덱스는 Volume 없을 수도)
4. **4순위**: POA 라이브러리 심볼 제약

## 🚨 긴급 체크 리스트

### DXY 완전 실패 원인 (0건)

- [ ] `detect_contract_value()`에 "DXY" 문자열 검색 로직 없음?
- [ ] `cv = 1.0` (기본값) 사용 중?
- [ ] Volume 필터가 DXY를 100% 차단?
- [ ] DXY 데이터 자체가 없음?

### GC1! 부분 작동 원인 (10건만)

- [ ] "GC1!" vs "GC" 매칭 실패?
- [ ] `cv` 값이 너무 크거나 작음?
- [ ] 특정 기간만 데이터 존재?
- [ ] 필터가 대부분 차단?

### USTEC.F / CL1! 정상 작동 원인

- [ ] 심볼 매칭 정상
- [ ] `cv` 값 정확
- [ ] 데이터 정상
- [ ] 필터 통과율 적절

## 💡 개선 방안 제안

### 제안 1: 심볼 감지 강화

```pine
detect_contract_value() =>
    string sym = syminfo.ticker
    float cv = 1.0
    
    // 인덱스 추가!
    if str.contains(sym, "DXY") or sym == "DXY"
        cv := 1000.0  // 또는 적절한 값
    
    // 선물 코드 매칭 개선
    else if str.contains(sym, "GC") or sym == "GC1!" or sym == "GOLD"
        cv := 100.0
    
    cv
```

### 제안 2: 디버깅 로그 추가

```pine
var table debug_table = table.new(position.bottom_right, 2, 5)

if barstate.islast
    table.cell(debug_table, 0, 0, text="심볼")
    table.cell(debug_table, 1, 0, text=syminfo.ticker)
    table.cell(debug_table, 0, 1, text="계약가치")
    table.cell(debug_table, 1, 1, text=str.tostring(final_contract_value))
    table.cell(debug_table, 0, 2, text="마지막 수량")
    table.cell(debug_table, 1, 2, text=str.tostring(long_qty))
```

### 제안 3: 조건부 필터

```pine
// 인덱스는 Volume 필터 OFF
bool use_volume_filter = not str.contains(syminfo.ticker, "DXY")
vol_long_entry := use_volume_filter ? vol_long_entry : true
```

## 📊 기대 결과

분석 후:
- DXY 0건 → 원인 식별 (감지 실패? Volume 필터?)
- GC1! 10건 → 원인 식별 (매칭 실패? 계약 가치?)
- 모든 심볼 균등 작동 목표

---

**CodeRabbit님께**:
위 내용을 바탕으로 `볼린져밴드_돌파_Futures_Optimized.fine` 파일을 전체 분석해서:
1. 각 의심 포인트별 문제 여부 확인
2. DXY 0건의 정확한 원인
3. GC1! 부분 작동 원인
4. 해결 방안 제시

부탁드립니다! 🙏

