# 🚨 긴급: 선물 파산 버그 발견 및 수정

## 📸 사용자 백테스트 결과 분석

**차트**: WTI 원유 선물 (WTI1!)  
**기간**: 2021.01 ~ 2025.10  
**결과**: **-135,418,552,507 USD** (약 -1,354억 달러 손실!)  
**거래 횟수**: 4회  
**승률**: 75%

### ❌ 치명적 버그: 강제 1계약 진입

## 🔍 근본 원인

### 버그 코드 (라인 87-88)
```pine
// 최소 계약수 보정
if raw_qty > 0 and raw_qty < min_contracts
    raw_qty := float(min_contracts)  // ❌ 0.016 → 1.0으로 강제!
```

### 실제 계산 흐름

**WTI 원유 선물 예시**:

```
1. 설정값
   - 자본: $1,000
   - Entry Size: 100%
   - 시장유형: "선물" ✓
   - 계약가치: 1.0 (기본값) ← 문제!
   - 최소 계약수: 1
   - leverage: 0 ← 큰 문제!

2. 배팅 금액 계산
   amount = 1000 × 100% = $1,000

3. 계약 명목가치 계산
   close = 62 (WTI 가격)
   cv = 1.0 (기본값, 실제로는 1000이어야 함)
   contract_notional = 62 × 1.0 = $62

4. 순수 수량 계산
   raw_qty = 1000 / 62 = 16.129 계약

5. 최소 계약수 보정 (버그!)
   raw_qty(16.129) > min_contracts(1)이므로 통과
   → 16.129 계약 그대로 사용

6. 랏 스텝 내림
   lots_stepped = floor(16.129 / 0.1) × 0.1 = 161 × 0.1 = 16.1 계약

7. 최종 진입
   16.1 계약 진입!
   
8. 실제 포지션 가치 (올바른 계약가치 1000 기준)
   16.1 계약 × (62 × 1000) = 16.1 × $62,000 = $998,200
   
9. 레버리지 0
   마진: 없음 또는 매우 부족
   
10. 가격 변동 시
    -1포인트 변동 = 16.1계약 × $1,000/포인트 = -$16,100 손실
    -10포인트 변동 = -$161,000 손실
    → 계좌 파산!
```

### 왜 이런 결과가?

1. **계약가치 기본값(1.0) 사용**:
   - 실제 WTI는 $1,000/포인트
   - 기본값 사용 시 1,000배 작게 계산됨
   - 결과: 수량이 1,000배 많아짐

2. **leverage: 0**:
   - 선물은 반드시 레버리지 필요
   - leverage=0은 마진 없이 거래 시도
   - 결과: 마진콜 또는 청산

3. **min_contracts 로직**:
   - 의도: "최소 1계약 이상"
   - 실제: 작은 수량을 1.0으로 강제 상향
   - 결과: 자금 초과 포지션 진입

## ✅ 수정 내용

### 1. min_contracts 강제 로직 완전 제거

**수정 전**:
```pine
if raw_qty > 0 and raw_qty < min_contracts
    raw_qty := float(min_contracts)  // 강제 상향
```

**수정 후**:
```pine
// 삭제: min_contracts 강제 로직
// 오직 스텝 계산 후 0이면 최소 랏으로만 보정
```

### 2. 계산 로직 단순화

```pine
// 순수 수량 계산
raw_qty = amount / contract_notional

// 랏 스텝 내림
lots_stepped = floor(raw_qty / futures_lot_step) × futures_lot_step

// 0이면 최소 랏 보정
if lots_stepped <= 0 and raw_qty > 0
    lots := futures_min_lot

// 최종 검증
return lots >= futures_min_lot ? lots : 0
```

## 📊 올바른 계산 예시

### WTI 원유 선물 (올바른 설정)

**설정**:
- 자본: $1,000
- Entry Size: 100%
- 시장유형: "선물"
- **계약가치: 1000** ⚠️ (중요!)
- 랏스텝: 0.1
- 최소랏: 0.1
- **leverage: 10** ⚠️ (중요!)

**계산**:
```
1. amount = $1,000

2. contract_notional = 62 × 1000 = $62,000

3. raw_qty = 1000 / 62000 = 0.016 계약

4. lots_stepped = floor(0.016 / 0.1) × 0.1 = 0 × 0.1 = 0

5. lots = 0.1 (최소 랏 보정)

6. 최종 진입: 0.1 계약 ✅

7. 포지션 가치: 0.1 × $62,000 = $6,200

8. 레버리지 10배
   필요 마진: $6,200 / 10 = $620
   남은 자금: $1,000 - $620 = $380 (안전!)

9. 1포인트 변동
   손익: 0.1계약 × $1,000/포인트 = ±$100
   (계좌의 10%, 관리 가능)
```

### 나스닥 마이크로 (MNQ)

**설정**:
- 자본: $1,000
- Entry Size: 100%
- **계약가치: 2**
- **leverage: 10**

**계산**:
```
1. amount = $1,000
2. contract_notional = 20000 × 2 = $40,000
3. raw_qty = 1000 / 40000 = 0.025
4. lots_stepped = 0
5. lots = 0.1 (최소 랏)
6. 포지션 가치: 0.1 × $40,000 = $4,000
7. 필요 마진 (10배): $400
8. 1포인트 손익: 0.1 × $2 = ±$0.20
```

## 🎯 사용자 액션 아이템

### 즉시 수정 필요!

1. **파일 재로드**:
   - 수정된 `볼린져밴드_돌파_Futures_Optimized.fine` 다시 로드
   
2. **WTI 원유 설정**:
   ```
   시장 유형: 선물
   계약 가치: 1000 ← 중요!
   랏 스텝: 0.1
   최소 랏: 0.1
   leverage: 10 이상 ← 필수!
   Entry Size: 10% (처음 테스트)
   ```

3. **백테스트 재실행**

4. **다른 선물 계약가치**:
   - MES (S&P500): 5
   - MNQ (나스닥): 2
   - GC (금): 100
   - SI (은): 5000
   - 6E (유로): 12500

### leverage 설정 가이드

**선물 거래는 반드시 레버리지 필요!**

- **보수적**: 5~10배
- **일반적**: 10~20배
- **공격적**: 20~50배

**절대 금지**: leverage = 0 ❌

포아봇 설정에서:
- `poa_leverage = 0` → 거래소 기본값 사용 (보통 20배)
- `poa_leverage = 10` → 10배 고정

## 🧪 테스트 검증

### 체크리스트

백테스트 후 다음 확인:

1. [ ] 총 손실이 초기 자본을 크게 초과하지 않는가?
2. [ ] 거래 횟수가 합리적인가? (수백~수천 건)
3. [ ] 포지션 사이즈가 합리적인가? (0.1~1.0 계약 수준)
4. [ ] 승률과 손익 비율이 정상적인가?
5. [ ] 월별 수익률이 ±50% 이내인가?

### 정상 vs 비정상 결과

**정상 예시**:
```
총 손익: +$500 ~ +$50,000
거래 횟수: 100~1,000회
승률: 40~70%
최대 낙폭: 10~30%
```

**비정상 예시 (버그)**:
```
총 손익: -$100,000,000+ ← 억 단위 손실
거래 횟수: 1~10회 ← 너무 적음
최대 낙폭: 100%+ ← 계좌 파산
```

## 📝 min_contracts 파라미터 처리

현재 `min_contracts` 입력은 더 이상 사용되지 않습니다.

### 옵션 1: 파라미터 제거 (권장)
```pine
// 삭제
min_contracts = input.int(1, "최소 계약 수(선물)", ...)
```

### 옵션 2: 도구팁 변경 (현재 유지)
```pine
min_contracts = input.int(1, "최소 계약 수(선물)", 
    tooltip="현재 미사용. 최소 랏은 '선물 최소 랏' 설정 사용")
```

다음 버전에서 완전 제거 예정.

## ✅ 결론

**버그 원인**:
1. ❌ min_contracts 강제 상향 로직
2. ❌ 계약가치 기본값(1.0) 사용
3. ❌ leverage = 0 설정

**수정 사항**:
1. ✅ min_contracts 강제 로직 제거
2. ✅ 주석 및 경고 추가
3. ⚠️ 사용자는 올바른 계약가치 입력 필수
4. ⚠️ leverage 10 이상 설정 필수

**기대 효과**:
- 모든 선물에서 최소 0.1 랏 진입
- 자금 부족 시 진입 안 함 (0 반환)
- 파산 방지

---

**수정 일시**: 2025-10-24  
**긴급도**: 🚨 CRITICAL  
**테스트 상태**: 수정 완료, 사용자 재테스트 필요

