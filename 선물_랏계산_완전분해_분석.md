# 🔬 선물 랏 계산 완전 분해 분석

## 🎯 핵심 발견

### 원본 코드 (정상 작동)

```pine
// Line 536
lot_size = validOpenLongPosition ? long_size_type(long_type) : ...

// Line 549-550
if (validOpenLongPosition)
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", 
                    qty = math.round(lot_size / close, exchange_decimal))
    long_position_size := lot_size / close
```

**흐름**:
1. `lot_size` = USD 금액 (예: 1000 USD)
2. `qty` = `lot_size / close` (예: 1000 / 50000 = 0.02 BTC)
3. `long_position_size` = `lot_size / close` (0.02 BTC 저장)
4. **조건 없이 무조건 진입!**

### 수정본 코드 (선물 안 됨)

```pine
// Line 631-634
if (validOpenLongPosition)
    pstrategy.close(id = 'Short Entry', comment = 'TR Short', immediately = true)
    long_qty = calc_order_qty(true)
    if ((market_type == "선물" and long_qty >= futures_min_lot) or 
        (market_type == "코인" and long_qty > 0))
        pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
        long_position_size := long_qty
```

**흐름**:
1. `calc_order_qty(true)` 호출
2. 내부에서 `amount = long_size_type(long_type)` (USD 금액)
3. 선물 분기: `raw_qty = amount / (close × cv)`
4. 랏 스텝 내림: `lots_stepped = floor(raw_qty / step) × step`
5. **조건 검사**: `long_qty >= futures_min_lot`
6. **실패 시 진입 안 함!**

## 🚨 문제점 1: 이중 조건 검사

### 원본
```pine
if (validOpenLongPosition)  // 조건 1개
    pstrategy.entry(...)    // 무조건 실행
```

### 수정본
```pine
if (validOpenLongPosition)              // 조건 1
    long_qty = calc_order_qty(true)
    if (long_qty >= futures_min_lot)    // 조건 2 (추가!)
        pstrategy.entry(...)
```

**결과**: 선물에서 `long_qty < futures_min_lot`이면 진입 차단!

## 🚨 문제점 2: 랏 스텝 내림의 함정

### 시나리오: XAUUSD (금)

**설정**:
- capital: $1,000
- long_size: 1 (= $1,000 배팅)
- close: $2,660 (금 현재가)
- contract_value: 100.0 (자동 감지)
- futures_lot_step: 0.1
- futures_min_lot: 0.1

**계산**:
```
amount = 1000 USD
contract_notional = 2660 × 100 = 266,000 USD
raw_qty = 1000 / 266,000 = 0.00376 계약
lots_stepped = floor(0.00376 / 0.1) × 0.1 = 0 계약
lots = 0 (lots_stepped가 0이므로)

조건: lots >= 0.1?
0 >= 0.1 → FALSE!
→ 진입 안 됨!
```

### 시나리오: MNQ (나스닥 마이크로)

**설정**:
- capital: $1,000
- long_size: 1 (= $1,000 배팅)
- close: $21,500
- contract_value: 2.0 (자동 감지)
- futures_lot_step: 0.1
- futures_min_lot: 0.1

**계산**:
```
amount = 1000 USD
contract_notional = 21500 × 2 = 43,000 USD
raw_qty = 1000 / 43,000 = 0.0233 계약
lots_stepped = floor(0.0233 / 0.1) × 0.1 = 0 계약
lots = 0

조건: lots >= 0.1?
0 >= 0.1 → FALSE!
→ 진입 안 됨!
```

### 시나리오: WTI (원유)

**설정**:
- capital: $1,000
- long_size: 1 (= $1,000 배팅)
- close: $70
- contract_value: 1000.0 (자동 감지)
- futures_lot_step: 0.1
- futures_min_lot: 0.1

**계산**:
```
amount = 1000 USD
contract_notional = 70 × 1000 = 70,000 USD
raw_qty = 1000 / 70,000 = 0.0143 계약
lots_stepped = floor(0.0143 / 0.1) × 0.1 = 0 계약
lots = 0

조건: lots >= 0.1?
0 >= 0.1 → FALSE!
→ 진입 안 됨!
```

## 🔍 왜 일부 선물은 작동했나?

### 작동한 경우: 나스닥 (가끔)

**추측**:
- close가 낮았거나
- long_size를 크게 설정했거나
- contract_value가 잘못 감지됐거나

**예**: long_size=10 (=$10,000 배팅)
```
amount = 10000 USD
contract_notional = 21500 × 2 = 43,000 USD
raw_qty = 10000 / 43,000 = 0.233 계약
lots_stepped = floor(0.233 / 0.1) × 0.1 = 0.2 계약
lots = 0.2

조건: 0.2 >= 0.1 → TRUE! ✅
→ 진입!
```

## 🚨 문제점 3: calc_order_qty의 보정 로직

### 현재 코드 (Line 187-188)

```pine
if lots <= 0 and raw_qty > 0
    lots := futures_min_lot
```

**의도**: raw_qty가 양수면 최소 랏으로 진입

**실제**:
- `lots_stepped = 0`이면 `lots = 0.1` (futures_min_lot)
- 하지만 **그 다음 조건 검사**에서 막힘!

```pine
lots >= futures_min_lot ? lots : 0
```

- `lots = 0.1`, `futures_min_lot = 0.1`
- `0.1 >= 0.1` → TRUE
- **반환: 0.1** ✅

그런데 왜 안 됐을까?

## 🔬 진짜 버그 발견!

### calc_order_qty 반환값 확인

```pine
// Line 191
lots >= futures_min_lot ? lots : 0
```

- `lots = 0.1` (보정됨)
- `futures_min_lot = 0.1`
- 반환: `0.1` ✅

### 진입 조건 확인

```pine
// Line 632
if ((market_type == "선물" and long_qty >= futures_min_lot) or ...)
```

- `long_qty = 0.1`
- `futures_min_lot = 0.1`
- `0.1 >= 0.1` → TRUE ✅
- **진입해야 함!**

## 🤯 그렇다면 왜?!

### 가설 1: `calc_order_qty`가 0을 반환

**점검**:
```pine
lots_stepped = math.floor(raw_qty / futures_lot_step) * futures_lot_step
```

- `raw_qty = 0.00376`
- `futures_lot_step = 0.1`
- `floor(0.00376 / 0.1) = floor(0.0376) = 0`
- `lots_stepped = 0 × 0.1 = 0`

**보정**:
```pine
if lots <= 0 and raw_qty > 0
    lots := futures_min_lot  // 0.1
```

- `lots = 0` (<=0이므로)
- `raw_qty = 0.00376` (>0이므로)
- `lots = 0.1` ✅

**최종 반환**:
```pine
lots >= futures_min_lot ? lots : 0
```
- `0.1 >= 0.1` → TRUE
- 반환: `0.1` ✅

### 가설 2: `futures_min_lot` 설정 문제

**사용자 설정 확인 필요**:
- 기본값: 0.1
- 사용자가 1.0으로 변경?

**만약 `futures_min_lot = 1.0`이면**:
```
calc_order_qty 반환: 0.1
진입 조건: 0.1 >= 1.0? → FALSE! ❌
→ 진입 안 됨!
```

## 💡 진단 계획

### 1단계: calc_order_qty 반환값 로그

```pine
calc_order_qty(isLong) =>
    float amount = isLong ? long_size_type(long_type) : short_size_type(short_type)
    
    if market_type == "코인"
        math.round(amount / close, exchange_decimal)
    else
        cv = get_contract_value()
        contract_notional = close * cv
        raw_qty = amount / contract_notional
        lots_stepped = math.floor(raw_qty / futures_lot_step) * futures_lot_step
        lots = lots_stepped
        if lots <= 0 and raw_qty > 0
            lots := futures_min_lot
        
        result = lots >= futures_min_lot ? lots : 0
        
        // 디버깅 로그 (테이블에 표시)
        // amount, raw_qty, lots_stepped, lots, result
        
        result
```

### 2단계: 진입 조건 직전 로그

```pine
if (validOpenLongPosition)
    long_qty = calc_order_qty(true)
    // 로그: long_qty 값, futures_min_lot 값
    if ((market_type == "선물" and long_qty >= futures_min_lot) or ...)
        pstrategy.entry(...)
```

### 3단계: 테이블 디버깅 정보 추가

```pine
table.cell(..., text="수량계산: amount=" + str.tostring(amount) + 
                     " raw=" + str.tostring(raw_qty) + 
                     " stepped=" + str.tostring(lots_stepped) + 
                     " final=" + str.tostring(long_qty))
```

## 🎯 최종 결론

**의심 1**: `futures_min_lot` 설정이 1.0으로 되어 있음
- 기본값 0.1인데 사용자가 변경?
- 또는 코드 어딘가에서 1.0으로 초기화?

**의심 2**: `calc_order_qty`가 실제로 0을 반환
- 보정 로직이 작동 안 함
- `raw_qty <= 0`인 경우?

**의심 3**: `long_qty` 변수가 덮어써짐
- `calc_order_qty` 이후에 다른 곳에서 수정?

**의심 4**: POA 라이브러리 내부 검증
- `pstrategy.entry`가 내부적으로 수량 검증?
- 최소 주문 금액 미달로 거부?

## 🔧 즉시 적용할 해결책

### 해결책 1: 진입 조건 제거

```pine
if (validOpenLongPosition)
    long_qty = calc_order_qty(true)
    // 조건 제거! 원본처럼 무조건 진입!
    pstrategy.entry(id = 'Long Entry', direction = "strategy.long", qty = long_qty)
    long_position_size := long_qty
```

### 해결책 2: calc_order_qty 강제 보정

```pine
calc_order_qty(isLong) =>
    ...
    else
        ...
        // 무조건 최소 랏 이상 반환 (0 방지!)
        lots = lots_stepped
        if lots < futures_min_lot
            lots := futures_min_lot  // 조건 완화!
        lots  // 조건 없이 반환!
```

### 해결책 3: 디버깅 모드 강화

```pine
// 전역 변수로 로그
var float debug_amount = na
var float debug_raw_qty = na
var float debug_final_qty = na

calc_order_qty(isLong) =>
    ...
    debug_amount := amount
    debug_raw_qty := raw_qty
    debug_final_qty := lots
    ...
```

---

**다음 단계**: 해결책 1 먼저 적용 (가장 빠른 해결)

